general {
    lock_cmd = /home/user/.config/hypr/lock.sh
    before_sleep_cmd = loginctl lock-session
}

listener {
    timeout = 300                                 # 5 min d'inactivité
    on-timeout = loginctl lock-session
}
