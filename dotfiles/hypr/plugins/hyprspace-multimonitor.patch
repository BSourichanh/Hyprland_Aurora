diff --git a/src/Input.cpp b/src/Input.cpp
index 6e3383f..b06f395 100644
--- a/src/Input.cpp
+++ b/src/Input.cpp
@@ -68,7 +68,7 @@ bool CHyprspaceWidget::buttonEvent(bool pressed, Vector2D coords) {
             targetWindow->finishAnimation();
         }
         if (Config::switchOnDrop) {
-            const auto pDropMonitor = State::monitorState()->query().id(targetWorkspace->m_monitor->m_id).run();
+            const auto pDropMonitor = State::monitorState()->query().id(targetWorkspace->monitorID()).run();
             if (pDropMonitor) pDropMonitor->changeWorkspace(targetWorkspace->m_id);
             if (Config::exitOnSwitch && active) hide();
         }
@@ -79,7 +79,7 @@ bool CHyprspaceWidget::buttonEvent(bool pressed, Vector2D coords) {
         if (targetWorkspace->m_isSpecialWorkspace)
             owner->activeSpecialWorkspaceID() == targetWorkspaceID ? owner->setSpecialWorkspace(nullptr) : owner->setSpecialWorkspace(targetWorkspaceID);
         else {
-            const auto pDropMonitor = State::monitorState()->query().id(targetWorkspace->m_monitor->m_id).run();
+            const auto pDropMonitor = State::monitorState()->query().id(targetWorkspace->monitorID()).run();
             if (pDropMonitor) pDropMonitor->changeWorkspace(targetWorkspace->m_id);
         }
         if (Config::exitOnSwitch && active) hide();
diff --git a/src/Layout.cpp b/src/Layout.cpp
index 029b763..80a628e 100644
--- a/src/Layout.cpp
+++ b/src/Layout.cpp
@@ -45,11 +45,11 @@ void CHyprspaceWidget::updateLayout() {
         if (!oActiveWorkspace) return;
 
         for (auto& ws : State::workspaceState()->workspaces()) { // HACK: recalculate other workspaces without reserved area
-            if (ws && ws->m_monitor && ws->m_monitor->m_id == ownerID && ws->m_id != oActiveWorkspace->m_id) {
+            if (ws && ws->monitorID() == ownerID && ws->m_id != oActiveWorkspace->m_id) {
                 pMonitor->m_activeWorkspace = ws.lock();
                 const auto curRules = std::to_string(pMonitor->activeWorkspaceID()) + ", gapsin:" + PGAPSIN->toString() + ", gapsout:" + PGAPSOUT->toString();
                 if (Config::overrideGaps) {
-                    if (const auto legacy = Config::Legacy::mgr().lock())
+                    if (const auto legacy = Config::Legacy::mgr().get())
                         legacy->handleWorkspaceRules("", curRules);
                 }
                 g_layoutManager->recalculateMonitor(pMonitor);
@@ -59,7 +59,7 @@ void CHyprspaceWidget::updateLayout() {
 
         const auto curRules = std::to_string(pMonitor->activeWorkspaceID()) + ", gapsin:" + std::to_string(Config::gapsIn) + ", gapsout:" + std::to_string(Config::gapsOut);
         if (Config::overrideGaps) {
-            if (const auto legacy = Config::Legacy::mgr().lock())
+            if (const auto legacy = Config::Legacy::mgr().get())
                 legacy->handleWorkspaceRules("", curRules);
         }
         g_layoutManager->recalculateMonitor(pMonitor);
@@ -67,10 +67,10 @@ void CHyprspaceWidget::updateLayout() {
     }
     else {
         for (auto& ws : State::workspaceState()->workspaces()) {
-            if (ws && ws->m_monitor && ws->m_monitor->m_id == ownerID) {
+            if (ws && ws->monitorID() == ownerID) {
                 const auto curRules = std::to_string(ws->m_id) + ", gapsin:" + PGAPSIN->toString() + ", gapsout:" + PGAPSOUT->toString();
                 if (Config::overrideGaps) {
-                    if (const auto legacy = Config::Legacy::mgr().lock())
+                    if (const auto legacy = Config::Legacy::mgr().get())
                         legacy->handleWorkspaceRules("", curRules);
                 }
             }
diff --git a/src/Overview.cpp b/src/Overview.cpp
index 24148c2..b833345 100644
--- a/src/Overview.cpp
+++ b/src/Overview.cpp
@@ -40,7 +40,7 @@ void CHyprspaceWidget::show() {
         // NOTE: workspace-level fullscreen tracking (m_fullscreenMode/getFullscreenWindow on CWorkspace)
         // was removed and centralized into Fullscreen::controller(); query per-workspace/per-window instead.
         for (auto& ws : State::workspaceState()->workspaces()) {
-            if (ws && ws->m_monitor && ws->m_monitor->m_id == ownerID) {
+            if (ws && ws->monitorID() == ownerID) {
                 const auto w = Fullscreen::controller()->getFullscreenWindow(ws.lock());
                 if (w != nullptr && Fullscreen::controller()->hasFullscreen(ws.lock())) {
                     const auto oMode = Fullscreen::controller()->getFullscreenModes(w).internal;
diff --git a/src/Render.cpp b/src/Render.cpp
index 6c94986..22b5ee0 100644
--- a/src/Render.cpp
+++ b/src/Render.cpp
@@ -1,3 +1,4 @@
+#include <hyprland/src/config/shared/workspace/WorkspaceRuleManager.hpp>
 #include "Overview.hpp"
 #include "Globals.hpp"
 #include <hyprland/src/helpers/memory/Memory.hpp>
@@ -219,47 +220,103 @@ void CHyprspaceWidget::draw() {
         workspaces.push_back(SPECIAL_WORKSPACE_START);
     }
 
-    // find the lowest and highest workspace id to determine which empty workspaces to insert
-    int lowestID = INT_MAX;
-    int highestID = 1;
+    auto isWorkspaceForThisMonitor = [&](int wsID) -> bool {
+        if (wsID < 1) return false;
+        const auto boundMon = Config::workspaceRuleMgr()->getBoundMonitorStringForWS(std::to_string(wsID));
+        if (!boundMon.empty()) {
+            return boundMon == owner->m_name;
+        }
+        const auto pWs = State::workspaceState()->query().id(wsID).run();
+        if (pWs != nullptr) {
+            return pWs->monitorID() == ownerID;
+        }
+        if (owner->m_name == "DP-1" || ownerID == 0) {
+            return wsID >= 6 && wsID <= 10;
+        } else {
+            return wsID >= 1 && wsID <= 5;
+        }
+    };
+
+    // Determine base workspace ID for this monitor
+    int baseID = (owner->m_name == "DP-1" || ownerID == 0) ? 6 : 1;
+
+    // 1. Gather all active / occupied workspaces for THIS monitor
     for (auto& ws : State::workspaceState()->workspaces()) {
         if (!ws) continue;
-        // normal workspaces start from 1, special workspaces ends on -2
         if (ws->m_id < 1) continue;
-        if (ws->m_monitor->m_id == ownerID) {
-            workspaces.push_back(ws->m_id);
-            if (highestID < ws->m_id) highestID = ws->m_id;
-            if (lowestID > ws->m_id) lowestID = ws->m_id;
+        if (isWorkspaceForThisMonitor(ws->m_id)) {
+            // Only show workspaces with windows, or the currently active workspace of this monitor
+            if (ws->getWindowCount() > 0 || ws == owner->m_activeWorkspace) {
+                workspaces.push_back(ws->m_id);
+            }
         }
     }
 
-    // include empty workspaces that are between non-empty ones
-    if (Config::showEmptyWorkspace) {
-        int wsIDStart = 1;
-        int wsIDEnd = highestID;
+    // Ensure the current active workspace is included if it belongs to this monitor
+    if (owner->m_activeWorkspace && isWorkspaceForThisMonitor(owner->m_activeWorkspace->m_id)) {
+        if (std::find(workspaces.begin(), workspaces.end(), owner->m_activeWorkspace->m_id) == workspaces.end()) {
+            workspaces.push_back(owner->m_activeWorkspace->m_id);
+        }
+    }
+
+    if (workspaces.empty()) {
+        workspaces.push_back(baseID);
+    }
 
-        // hyprsplit/split-monitor-workspaces compatibility
-        if (numWorkspaces > 0) {
-            wsIDStart = std::min<int>(numWorkspaces * ownerID + 1, lowestID);
-            wsIDEnd = std::max<int>(numWorkspaces * ownerID + 1, highestID); // always show the initial workspace for current monitor
+    // 2. Add exactly ONE empty workspace at the end (active + 1 empty for new workspace)
+    bool hasEmptyWorkspace = false;
+    for (int wsID : workspaces) {
+        const auto pWs = State::workspaceState()->query().id(wsID).run();
+        if (pWs == nullptr || pWs->getWindowCount() == 0) {
+            hasEmptyWorkspace = true;
+            break;
         }
+    }
 
-        for (int i = wsIDStart; i <= wsIDEnd; i++) {
-            if (i == owner->activeSpecialWorkspaceID()) continue;
-            const auto pWorkspace = State::workspaceState()->query().id(i).run();
-            if (pWorkspace == nullptr)
-                workspaces.push_back(i);
+    if (Config::showNewWorkspace && !hasEmptyWorkspace) {
+        int maxOccupied = baseID;
+        for (int id : workspaces) {
+            if (id > maxOccupied) maxOccupied = id;
+        }
+        int candidate = maxOccupied + 1;
+        bool added = false;
+        for (int offset = 0; offset < 20; offset++) {
+            int testId = candidate + offset;
+            if (isWorkspaceForThisMonitor(testId)) {
+                if (std::find(workspaces.begin(), workspaces.end(), testId) == workspaces.end()) {
+                    workspaces.push_back(testId);
+                    added = true;
+                    break;
+                }
+            }
+        }
+        if (!added) {
+            for (int testId = baseID; testId < baseID + 10; testId++) {
+                if (isWorkspaceForThisMonitor(testId)) {
+                    if (std::find(workspaces.begin(), workspaces.end(), testId) == workspaces.end()) {
+                        workspaces.push_back(testId);
+                        break;
+                    }
+                }
+            }
         }
     }
 
-    // add a new empty workspace at last
-    if (Config::showNewWorkspace) {
-        // get the lowest empty workspce id after the highest id of current workspace
-        while (State::workspaceState()->query().id(highestID).run() != nullptr) highestID++;
-        workspaces.push_back(highestID);
+    // Strict safety filter: ensure NO workspace from another monitor ever leaks into this overview
+    std::vector<int> filtered;
+    for (int wsID : workspaces) {
+        if (wsID == SPECIAL_WORKSPACE_START && Config::showSpecialWorkspace) {
+            filtered.push_back(wsID);
+            continue;
+        }
+        if (isWorkspaceForThisMonitor(wsID)) {
+            filtered.push_back(wsID);
+        }
     }
+    workspaces = std::move(filtered);
 
     std::sort(workspaces.begin(), workspaces.end());
+    workspaces.erase(std::unique(workspaces.begin(), workspaces.end()), workspaces.end());
 
     // render workspace boxes
     int wsCount = workspaces.size();
diff --git a/src/main.cpp b/src/main.cpp
index e53d994..f6f6245 100644
--- a/src/main.cpp
+++ b/src/main.cpp
@@ -8,6 +8,7 @@
 #include <hyprland/src/desktop/view/Window.hpp>
 #include <hyprland/src/state/MonitorState.hpp>
 #include <hyprland/src/pointer/PointerController.hpp>
+#include <hyprland/src/desktop/state/FocusState.hpp>
 #include <hyprutils/memory/SharedPtr.hpp>
 #include <any>
 #include "Overview.hpp"
@@ -173,7 +174,7 @@ void onWorkspaceChange(PHLWORKSPACE pWorkspace) {
 
     if (!pWorkspace) return;
 
-    auto widget = getWidgetForMonitor(State::monitorState()->query().id(pWorkspace->m_monitor->m_id).run());
+    auto widget = getWidgetForMonitor(State::monitorState()->query().id(pWorkspace->monitorID()).run());
     if (widget != nullptr)
         if (widget->isActive())
             widget->show();
@@ -328,7 +329,9 @@ void onTouchUp(const ITouch::SUpEvent& event, SCallbackInfo& info) {
 }
 
 static SDispatchResult dispatchToggleOverview(std::string arg) {
-    auto currentMonitor = State::monitorState()->query().vec(g_pInputManager->getMouseCoordsInternal()).run();
+    auto currentMonitor = Desktop::focusState()->monitor();
+    if (!currentMonitor)
+        currentMonitor = State::monitorState()->query().vec(g_pInputManager->getMouseCoordsInternal()).run();
     auto widget = getWidgetForMonitor(currentMonitor);
     if (widget) {
         if (arg.contains("all")) {
@@ -360,7 +363,9 @@ static SDispatchResult dispatchOpenOverview(std::string arg) {
         }
     }
     else {
-        auto currentMonitor = State::monitorState()->query().vec(g_pInputManager->getMouseCoordsInternal()).run();
+        auto currentMonitor = Desktop::focusState()->monitor();
+        if (!currentMonitor)
+            currentMonitor = State::monitorState()->query().vec(g_pInputManager->getMouseCoordsInternal()).run();
         auto widget = getWidgetForMonitor(currentMonitor);
         if (widget)
             if (!widget->isActive()) widget->show();
@@ -375,7 +380,9 @@ static SDispatchResult dispatchCloseOverview(std::string arg) {
         }
     }
     else {
-        auto currentMonitor = State::monitorState()->query().vec(g_pInputManager->getMouseCoordsInternal()).run();
+        auto currentMonitor = Desktop::focusState()->monitor();
+        if (!currentMonitor)
+            currentMonitor = State::monitorState()->query().vec(g_pInputManager->getMouseCoordsInternal()).run();
         auto widget = getWidgetForMonitor(currentMonitor);
         if (widget)
             if (widget->isActive()) widget->hide();
